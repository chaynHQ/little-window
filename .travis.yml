language: node_js
node_js:
    - "node"
notifications:
    -  email: false
services:
    - postgresql
before_script:
    - psql -c 'create database littlewindowtest;' -U postgres
script:
  - npm run lint
  - npm run test
  - cd client && npm test --passWithNoTests
  - npm run lint
